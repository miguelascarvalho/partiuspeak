name: partiuspeak
description: "Versão exclusiva para iOS e macOS do app PartiuSpeak"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Interface e estilo Apple
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2
  provider: ^6.1.5+1
  url_launcher: ^6.3.2
  http: ^1.5.0

  # Armazenamento local e utilitários
  shared_preferences: ^2.5.3
  image_picker: ^1.2.0
  path_provider: ^2.1.5
  flutter_tts: ^4.2.3
  dio: ^5.9.0
  translator: ^1.0.4+1

  # 🔥 Firebase — versões reais e compatíveis (com base no seu pubspec.lock)
  firebase_core: ^4.2.1
  firebase_analytics: ^12.0.4
  firebase_auth: ^6.1.2
  firebase_storage: ^13.0.4
  cloud_firestore: ^6.1.0
  firebase_app_check: ^0.4.1+2
  firebase_crashlytics: ^5.0.4

  # Login Apple e datas
  sign_in_with_apple: ^7.0.1
  intl: ^0.20.2

  # Compras no app (iOS)
  in_app_purchase: ^3.2.3
  crypto: ^3.0.7
  audioplayers: ^6.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7
  build_runner: ^2.10.1
  json_serializable: ^6.11.1

flutter_launcher_icons:
  ios: true
  image_path: "assets/images/LogoPSpk.png"

flutter_native_splash:
  color: "#A5D6A7"
  image: assets/images/LogoPSpk.png
  ios: true
  web: false

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/fonts/
    - assets/audio/
    - assets/data/
    - assets/data/module_metadata.json
    - assets/data/modulo_zero/
    - assets/data/modulo_iniciante/
    - assets/data/modulo_intermediario/
    - assets/data/modulo_avancado/
    - assets/images/LogoPSpk.png

  fonts:
    - family: Montserrat
      fonts:
        - asset: assets/fonts/Montserrat-Regular.ttf
